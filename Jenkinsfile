pipeline{
	agent any{
		
	stages {
		
		stage (build) {
			
			steps {
				echo "bilding in this stage"
			}
		}
			
		stage (test) {
			
			steps {
				echo "testing the build"
			}
		}
			
		stage (deploy) {
			
			steps {
				echo "deployed the build"
			}
		}
				
		}
	}
}
